# Example: lead kickoff for TREX

- approved reference: <fill>
- board: trex-stagepilot
- goal: <fill>
- escalation target: trex-dev-lead

Note: this board card is the root kickoff example. Follow-on `delivery-runner -> dev-impl` and `delivery-runner -> dev-qc` handoffs are ordinary non-kanban handoffs by default unless the project overlay says otherwise.
